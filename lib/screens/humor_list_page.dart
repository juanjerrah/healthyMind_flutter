import 'package:flutter/material.dart';

class HumorListPage extends StatelessWidget {
  const HumorListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Tela Listagem Humores"),
    );
  }
}